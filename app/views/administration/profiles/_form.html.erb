<%= form_for(@administration_profile) do |f| %>
  <% if @administration_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@administration_profile.errors.count, "error") %> prohibited this administration_profile from being saved:</h2>

      <ul>
      <% @administration_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
