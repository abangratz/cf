<h1>Listing articles</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th>Body html</th>
    <th>Conversion</th>
    <th>Slug</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @articles.each do |article| %>
  <tr>
    <td><%= article.title %></td>
    <td><%= article.slug %></td>
    <td><%= link_to 'Show', administration_article_path(article) %></td>
    <td><%= link_to 'Edit', edit_administration_article_path(article) %></td>
    <td><%= link_to 'Destroy', administration_article_path(article), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Article', new_administration_article_path %>
